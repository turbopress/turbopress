---
import type { Layout } from "@turbopress/api/types";
import RenderHeader from "./RenderHeader.astro";
interface Props {
  layout: Layout;
}

const { layout } = Astro.props;
---

<RenderHeader blocks={layout.header?.blocks} />
